# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Debug Firebase Service Account Presence
        run: |
          if [ -z "${{ secrets.FIREBASE_SERVICE_ACCOUNT_INVENTORY_USER123 }}" ]; then
            echo "❌ firebaseServiceAccount secret is EMPTY"
            exit 1
          else
            echo "✅ firebaseServiceAccount secret is present"
          fi
      # - run: npm install && npm run build
      # - uses: FirebaseExtended/action-hosting-deploy@v0
      #   with:
      #     repoToken: "${{ secrets.GITHUB_TOKEN }}"
      #     firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_INVENTORY_USER123 }}"
      #     projectId: inventory-user123
